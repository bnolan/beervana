<div class="beer inline-beer">
  <b class="rating" style="background-color: rgba(255,192,0,<%= beer.current_rating.average_rating / 5.0 %>)"><%= beer.current_rating.friendly_rating %></b>
  <h4><%= link_to beer.name, beer %></h4>
  <span class="brewery"><%= beer.brewery.name %></span>
</div>
